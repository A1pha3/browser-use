---
title: "代理基础"
description: "学习如何配置和创建浏览器自动化代理。"
icon: "bot"
mode: "wide"
---

<div className="mb-8">
  <span className="text-sm px-3 py-1 bg-green-100 text-green-800 rounded-full mr-2">⭐⭐ 基础级</span>
  <span className="text-sm px-3 py-1 bg-gray-100 text-gray-600 rounded-full">预计阅读: 30分钟</span>
  <span className="text-sm px-3 py-1 bg-purple-100 text-purple-800 rounded-full">Python 异步编程</span>
</div>

## 学习目标

完成本文档学习后，你将能够：

| 目标层级 | 描述 | 检验标准 |
|---------|------|---------|
| **记忆** | 说出 Agent 的核心参数和配置项 | 能列举 10+ Agent 参数 |
| **理解** | 解释 Agent 执行流程和各组件关系 | 能画出 Agent 工作流程图 |
| **应用** | 创建和配置满足需求的 Agent 实例 | 能独立完成自定义 Agent 配置 |

## 核心概念

`Agent` 是 Browser Use 的核心组件，负责接收任务、规划操作步骤并执行浏览器操作。

```
┌─────────────────────────────────────────────────────────────┐
│                    Agent 核心架构                             │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│   ┌─────────────────────────────────────────────────────┐   │
│   │                    Agent                             │   │
│   │   ├── task（任务描述）                              │   │
│   │   ├── llm（大语言模型）                             │   │
│   │   ├── browser（浏览器实例）                         │   │
│   │   └── tools（工具注册表）                           │   │
│   └─────────────────────────────────────────────────────┘   │
│                          ↓                                   │
│   ┌─────────────────────────────────────────────────────┐   │
│   │              Agent 执行循环                          │   │
│   │   1. 捕获页面状态（HTML + 截图）                    │   │
│   │   2. 发送给 LLM 分析                               │   │
│   │   3. LLM 返回下一个动作                            │   │
│   │   4. 执行动作并验证结果                            │   │
│   │   5. 重复直到任务完成或达到最大步数                  │   │
│   └─────────────────────────────────────────────────────┘   │
│                          ↓                                   │
│   ┌─────────────────────────────────────────────────────┐   │
│   │              返回 AgentHistoryList                   │   │
│   │   • 执行历史 • 截图 • 提取内容 • 错误信息            │   │
│   └─────────────────────────────────────────────────────┘   │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### Agent 与其他组件的关系

```python
from browser_use import Agent, Browser, Tools, ChatBrowserUse

# Browser 管理浏览器实例
browser = Browser(headless=False)

# Tools 管理可用工具
tools = Tools()

# Agent 协调所有组件
agent = Agent(
    task="搜索最新的 AI 新闻",
    llm=ChatBrowserUse(),  # 大脑
    browser=browser,       # 执行器
    tools=tools,          # 工具箱
)
```

## 基础用法

```python
from browser_use import Agent, ChatBrowserUse

# 创建代理
agent = Agent(
    task="搜索最新的 AI 新闻",
    llm=ChatBrowserUse(),
)

async def main():
    history = await agent.run(max_steps=100)
```

## 核心参数

### task（任务描述）

**描述**：用自然语言描述你要自动化的任务。

```python
agent = Agent(
    task="访问 https://quotes.toscrape.com/，提取所有名人名言和作者",
)
```

### llm（大语言模型）

**描述**：指定用于驱动代理的大语言模型。

```python
from browser_use import ChatBrowserUse, ChatOpenAI

# 使用 ChatBrowserUse（推荐）
agent = Agent(task="...", llm=ChatBrowserUse())

# 使用 OpenAI
agent = Agent(task="...", llm=ChatOpenAI(model="gpt-4o"))
```

### browser（浏览器实例）

**描述**：指定浏览器实例，如不指定则使用默认配置。

```python
from browser_use import Browser, Agent, ChatBrowserUse

browser = Browser(headless=False)
agent = Agent(task="...", browser=browser, llm=ChatBrowserUse())
```

## 执行方法

### run()

异步执行代理任务：

```python
history = await agent.run(
    max_steps=100,  # 最大步数（默认：100）
)
```

### 返回值

`run()` 返回 `AgentHistoryList` 对象，包含完整的执行历史：

```python
history = await agent.run()

# 访问各种信息
print(history.urls())                     # 访问过的 URL 列表
print(history.screenshot_paths())         # 截图路径列表
print(history.screenshots())              # 截图（base64 字符串）
print(history.action_names())             # 执行的操作名称
print(history.extracted_content())        # 提取的内容
print(history.errors())                   # 错误列表
print(history.model_actions())             # 所有操作及其参数
print(history.model_outputs())             # 所有模型输出
print(history.last_action())              # 最后一个操作

# 分析方法
print(history.final_result())              # 最终提取的内容
print(history.is_done())                  # 是否完成
print(history.is_successful())            # 是否成功
print(history.has_errors())               # 是否有错误
print(history.number_of_steps())           # 执行步数
print(history.total_duration_seconds())   # 总耗时（秒）
```

### History 数据结构心智模型

```
AgentHistoryList
├── 执行历史
│   ├── urls()           → [url1, url2, ...]
│   ├── screenshot_paths() → [path1, path2, ...]
│   ├── action_names()   → ['click', 'input', 'scroll', ...]
│   └── extracted_content() → [content1, content2, ...]
│
├── 状态判断
│   ├── is_done()        → 任务是否完成
│   ├── is_successful()  → 是否成功（未超步数）
│   └── has_errors()     → 是否有错误
│
└── 结果
    ├── final_result()   → 最终结果
    └── number_of_steps() → 执行步数
```

## 常用配置

### vision（视觉模式）

```python
agent = Agent(
    task="...",
    llm=ChatBrowserUse(),
    use_vision="auto",  # auto（默认）- 需要时使用截图
    # use_vision=True,   # 始终包含截图
    # use_vision=False,  # 从不包含截图（仅文本）
)
```

### 视觉详细程度

```python
agent = Agent(
    task="...",
    llm=ChatBrowserUse(),
    vision_detail_level="auto",  # auto（默认）
    # vision_detail_level="low",   # 低细节，快速处理
    # vision_detail_level="high",  # 高细节，精确分析
)
```

### 页面内容提取模型

```python
# 使用单独的模型进行页面内容提取
agent = Agent(
    task="...",
    llm=ChatBrowserUse(),                    # 主模型
    page_extraction_llm=ChatOpenAI(model="gpt-4o-mini"),  # 提取模型
)
```

<Note icon="key" color="#FFC107" iconType="regular">
**优化建议**：对于简单的页面提取任务，使用轻量级模型（如 gpt-4o-mini）可以显著降低成本。
</Note>

## 执行控制

```python
agent = Agent(
    task="...",
    llm=ChatBrowserUse(),

    max_steps=100,           # 最大执行步数
    max_actions_per_step=3,  # 每步最大操作数（表单填写时可合并多个输入）
    max_failures=3,          # 最大失败重试次数
    final_response_after_failure=True,  # 失败后尝试最终响应

    # 快速模式
    flash_mode=False,  # 跳过评估和思考过程，更快
)
```

### 执行控制心智模型

```
任务执行流程

开始
  ↓
重复直到完成或达到最大步数
  ├─ 捕获页面状态
  ├─ LLM 决策下一步
  ├─ 执行操作（最多 max_actions_per_step 个）
  ├─ 检查结果
  └─ 失败？ → 重试（最多 max_failures 次）
    ↓
任务完成 / 达到最大步数 / 重试用尽
  ↓
返回执行历史
```

## 系统提示词

### 覆盖默认提示词

```python
agent = Agent(
    task="...",
    llm=ChatBrowserUse(),
    override_system_message="""你是一个专业的浏览器自动化助手。
    规则：
    1. 始终等待页面完全加载后再操作
    2. 优先使用键盘导航
    3. 遇到错误时尝试备用方案
    """,
)
```

### 扩展默认提示词

```python
agent = Agent(
    task="...",
    llm=ChatBrowserUse(),
    extend_system_message="""额外规则：
    1. 填写表单时仔细检查所有字段
    2. 提交前确认数据正确性
    """,
)
```

<Warning>
**注意**：`override_system_message` 会完全替换默认提示词，可能影响 Agent 的基本能力。谨慎使用。
</Warning>

## 初始操作

```python
from browser_use import Agent, Browser, ChatBrowserUse

agent = Agent(
    task="...",
    llm=ChatBrowserUse(),
    browser=Browser(headless=False),
    initial_actions=[
        {'open_tab': {'url': 'https://example.com'}},
    ],
)
```

## 文件和数据管理

```python
agent = Agent(
    task="...",
    llm=ChatBrowserUse(),

    save_conversation_path='./conversation.json',  # 保存对话历史
    save_conversation_path_encoding='utf-8',        # 编码格式

    available_file_paths=['./data', './config'],   # 可访问的文件路径

    sensitive_data={                                # 敏感数据
        'api_key': 'your-key-here',
    },
)
```

## 视觉输出

```python
agent = Agent(
    task="...",
    llm=ChatBrowserUse(),

    generate_gif=False,  # 或设置为路径如 './animation.gif'
    include_attributes=['id', 'name', 'placeholder'],  # HTML 属性
)
```

## 超时控制

```python
agent = Agent(
    task="...",
    llm=ChatBrowserUse(),

    llm_timeout=90,      # LLM 调用超时（秒）
    step_timeout=120,     # 每步超时（秒）
)
```

## 内存管理

```python
agent = Agent(
    task="...",
    llm=ChatBrowserUse(),

    max_history_items=None,  # 保留的历史步骤数，None 表示全部保留
)
```

<Note>
对于长任务，限制 `max_history_items` 可以减少 Token 消耗，但可能影响 Agent 的上下文理解能力。
</Note>

## 直接打开 URL

```python
agent = Agent(
    task="...",
    llm=ChatBrowserUse(),

    directly_open_url=True,  # 检测到 URL 时直接打开（默认：True）
)
```

## 成本计算

```python
agent = Agent(
    task="...",
    llm=ChatBrowserUse(),

    calculate_cost=True,  # 计算和跟踪 API 成本
)
```

## 完整配置示例

```python
from browser_use import Agent, Browser, ChatBrowserUse

agent = Agent(
    # 核心配置
    task="你的任务描述",
    llm=ChatBrowserUse(),
    browser=Browser(headless=False),

    # 视觉
    use_vision="auto",
    vision_detail_level="auto",
    page_extraction_llm=None,

    # 执行控制
    max_steps=100,
    max_actions_per_step=3,
    max_failures=3,
    final_response_after_failure=True,
    flash_mode=False,

    # 系统提示词
    override_system_message=None,
    extend_system_message=None,

    # 初始操作
    initial_actions=None,

    # 文件和数据
    save_conversation_path=None,
    save_conversation_path_encoding='utf-8',
    available_file_paths=None,
    sensitive_data=None,

    # 视觉输出
    generate_gif=False,
    include_attributes=['id', 'name', 'placeholder', 'type', 'class'],

    # 性能和超时
    max_history_items=None,
    llm_timeout=90,
    step_timeout=120,
    directly_open_url=True,

    # 成本
    calculate_cost=False,

    # 显示文件信息
    display_files_in_done_text=True,
)

async def main():
    history = await agent.run()
```

## 实践练习

### 练习 1：创建第一个 Agent（⭐⭐）

```python
from browser_use import Agent, ChatBrowserUse

agent = Agent(
    task="访问 https://example.com，提取页面标题",
    llm=ChatBrowserUse(),
)
```

**练习目标**：运行 Agent，理解执行流程。

### 练习 2：配置执行控制（⭐⭐⭐）

```python
from browser_use import Agent, Browser, ChatBrowserUse

browser = Browser(headless=False)

agent = Agent(
    task="复杂的多步骤任务",
    llm=ChatBrowserUse(),
    browser=browser,
    max_steps=50,              # 限制最大步数
    max_actions_per_step=3,    # 每步可执行多个操作
    max_failures=5,            # 允许更多重试
    step_timeout=180,          # 每步超时 3 分钟
)
```

**练习目标**：配置适合复杂任务的参数。

### 练习 3：保存和分析执行历史（⭐⭐⭐）

```python
from browser_use import Agent, ChatBrowserUse

agent = Agent(
    task="数据采集任务",
    llm=ChatBrowserUse(),
    save_conversation_path='./history.json',
    calculate_cost=True,
)

history = await agent.run()

# 分析结果
print(f"执行步数: {history.number_of_steps()}")
print(f"总耗时: {history.total_duration_seconds()}秒")
print(f"访问的 URL: {history.urls()}")
print(f"提取的内容: {history.extracted_content()}")
print(f"最终结果: {history.final_result()}")
```

## 常见问题

### Q: Agent 运行太慢怎么办？

**A**: 尝试以下优化：

1. **启用快速模式**：`flash_mode=True`
2. **减少视觉详细程度**：`vision_detail_level="low"`
3. **限制历史记录**：`max_history_items=10`
4. **使用更快的模型**：如 `ChatBrowserUse` 或 `gemini-flash-latest`

### Q: 任务中途失败怎么办？

**A**: 增加重试次数和超时时间：

```python
agent = Agent(
    task="...",
    llm=ChatBrowserUse(),
    max_failures=5,
    final_response_after_failure=True,
    step_timeout=180,
)
```

### Q: 如何提高任务成功率？

**A**: 建议：

1. **任务描述要具体**：避免模糊的指令
2. **使用视觉模式**：`use_vision=True`
3. **增加步骤限制**：`max_steps` 设置足够大
4. **添加验证步骤**：在关键操作后检查结果

### Q: Agent 不按预期执行怎么办？

**A**:

1. **检查任务描述**：是否足够清晰和具体
2. **添加系统提示**：使用 `extend_system_message` 添加规则
3. **使用初始操作**：`initial_actions` 预设初始状态
4. **查看执行历史**：分析 Agent 的决策过程

## 相关资源

- [所有 Agent 参数](/customize/agent/all-parameters)
- [工具配置](/customize/tools/basics)
- [浏览器配置](/customize/browser/basics)

---

<div className="mt-8 p-4 bg-blue-50 border-l-4 border-blue-500 rounded">

**⭐ 学习检查**：完成本节后，你应该能：

- [ ] 说出 Agent 的核心参数和配置项
- [ ] 解释 Agent 执行流程和各组件关系
- [ ] 创建和配置满足需求的 Agent 实例
- [ ] 理解如何分析和利用执行历史

</div>
